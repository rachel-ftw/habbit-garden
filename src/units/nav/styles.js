import { css } from "@emotion/react";

const styles = {
  container: css`
    grid-area: nav;
    padding: 10px;
    display: flex;
    justify-content: space-between;
    flex-direction: row;
  `,
};

export default styles;
